# Test scratch (test-fork-count writes a sandbox under the cwd it runs from)
_fork_sandbox/

# Release build artifacts
/dist/

# Debian build artifacts
/debian/questlog/
/debian/files
/debian/debhelper-build-stamp
/debian/.debhelper/
/debian/*.substvars
/debian/*.debhelper
